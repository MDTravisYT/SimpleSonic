LEVEL:
		JSR		CLEARSCREEN

		MOVE.B	#$10,	(OBJSLOT00)
		LEA		(OBJSLOT00),	A0
		MOVE.W	#160,	OBJ.X(A0)
		MOVE.W	#112,	OBJ.Y(A0)
		MOVE.B	#$2,	(OBJSLOT01)
		MOVE.B	#$3,	(OBJSLOT02)
		
	@LOOP:
		MOVE.B	#4,	(VBLANKCMD).W
		JSR		VSYNC
		JSR		RUNOBJECTS
		JSR		PROCESSSPRITES
		BRA.S	@LOOP